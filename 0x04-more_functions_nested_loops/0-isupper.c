#include "main.h"

/**
 *0-isupper.c: checks upper or lower.
 *Return:  _isupper(y) returns 1 if' y'is upper cause if not returns 0.
 */
int _isupper(int y)
{
	 if (y >= 65 && y <= 90)
	{
		return (1);
	}
	return (0);
}
